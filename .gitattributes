# Auto detect text files and normalize line endings
* text=auto

# Explicitly declare text files we want to always normalize and convert to native line endings
*.js    text
*.json  text
*.env   text
*.md    text

# Denote all files that are truly binary and should not be modified
*.png   binary
*.jpg   binary
*.gif   binary
